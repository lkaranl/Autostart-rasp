# Autostart-rasp

Cursor do Mouse: Se quiser esconder o cursor do mouse enquanto a página está aberta, instale o utilitário unclutter:
`sudo apt install unclutter`

Criar ou editar o arquivo
`mkdir -p ~/.config/autostart`
`nano ~/.config/autostart/firefox_kiosk.desktop`